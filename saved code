<div id="nav">
			<ul>
				<li>{{ HTML::link_to_route('home', 'Home') }}</li>
				@if(!Auth::check())
					<li>{{ HTML::link_to_route('register', 'Register') }}</li>
					<li>{{ HTML::link_to_route('login', 'Login') }}</li>
				@else 
					<li>{{ HTML::link_to_route('your_questions', "Your graph") }}</li>
					<li>{{ HTML::link_to_route('logout', 'Logout ('.Auth::user()->username.')') }}</li>
				@endif
			</ul>
		</div><!-- end nav -->



@if(Session::has('message'))
				<p id="message">{{ Session::get('message') }}</p>
			@endif


<div id="searchbar">
				{{ Form::open('search', 'POST') }}

				{{ Form::token() }}

				{{ Form::text('keyword', 'Search', array('id'=>'keyword')) }}

				{{ Form::submit('Search') }}

				{{ Form::close() }}
			</div><!-- end searchbar -->


			<p id="message" class="navbar-text form-center">
					@if(Session::has('message'))
						{{ Session::get('message') }}
					@endif
			</p>

			@if(Session::has('message'))
	<script type="text/javascript">
		$('#message').fadeIn();
	</script>
@endif

#message {
	visibility: hidden;
}